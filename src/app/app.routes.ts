import { Routes } from '@angular/router';
import { LoginRegisterComponent } from './pages/login-register';

export const routes: Routes = [
    {
        path: '',
        component: LoginRegisterComponent
    }
];
